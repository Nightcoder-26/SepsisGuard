# External Validation Report

**EXTERNAL VALIDATION STATUS**: **`BLOCKED — independent dataset unavailable`**

PhysioNet Set A and Set B were included in Phase 2 development splits (`train.csv`, `val.csv`, `test.csv`). To prevent data leakage, no external validation was performed on development data copies.

The `external_validation.py` framework is implemented and ready to evaluate future independent cohorts.
